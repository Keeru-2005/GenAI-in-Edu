{
  "buildCommand": "cd genai-in-edu && npm install --legacy-peer-deps && CI=false npm run build",
  "outputDirectory": "genai-in-edu/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
